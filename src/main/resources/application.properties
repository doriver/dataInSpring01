spring.application.name=data

#spring.profiles.active=local

server.port=8091

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/dataex?characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=qlqjs

spring.jpa.hibernate.ddl-auto=update
# create : 애플리케이션 실행시점에 기존 테이블들을 모두 삭제하고, 다시 생성한다.
# update : 애플리케이션 실행시점에 ‘JPA 엔티티 설계’와 ‘실제 테이블 상태’를 비교하여, 실제 테이블에서 변경된 부분만 반영

spring.jpa.open-in-view=true
